# Benchmark

Benchmark plans, methodology, and summarized results live here.

Put raw or generated benchmark output under `docs/benchmark/results/` when it is small enough to track. Put large rebuildable benchmark artifacts under `build/` or `resources/cache/` instead.

Do not record private machine names, local absolute paths, user names, IP addresses, ports, or secret configuration in benchmark notes.
